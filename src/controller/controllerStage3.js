const banco = require("../config/banco");
const fire = require('../config/firebase-config') 

module.exports.changeAddressImp = async function (cond1, cond2) { 
   if( cond1 && cond2){
    
   }else{

   }
};